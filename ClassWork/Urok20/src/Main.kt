fun main(arg: Array<String>) { //вариант на Kotlin

//public static void main (String[] arg){ // вариант как в Java
//}
print("asd")
    val i = 50
    when(i){
        40 ->{

        }
    }
    for (a in arg){}
    val list: MutableList<String>  //val аналог final, var обычная переменная

    var t:  Int? = null ?: 25 //в конце проверка, если null, то подменит на 25
    t?.compareTo(2) // надо указать !! для вызова null или ? чтоб пропустить эту ошибку
}
fun test(value: Int){

}