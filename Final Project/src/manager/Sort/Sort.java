package manager.Sort;

import model.entity.Beer;

import java.util.Comparator;

public class Sort implements Comparator<Beer>{
    @Override
    public int compare(Beer o1, Beer o2) {
        return 0;
    }
}
